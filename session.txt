Moon Panel — Session Summary
What's Built
Go Auth Service (services/auth/)
- ✅ gRPC server with Register, Login, ValidateToken RPCs
- ✅ Password hashing (bcrypt) + JWT generation/validation
- ✅ PostgreSQL store (Create, GetByEmail, GetByID)
- ✅ Unit + integration tests
- ✅ Dockerfile (multi-stage build)
- Port: 50051 (configurable via GRPC_PORT)
BFF — NestJS (bff/)
- ✅ Auth module (bff/src/auth/auth.module.ts)
- ✅ Auth controller — POST /api/auth/login, /register, /validate-token
- ✅ gRPC client (bff/src/grpc/grpc.client.ts) with env var:
- GRPC_AUTH_ADDRESS — default: localhost:50051, Docker: auth:50051
- PROTO_PATH — default: relative ../../../proto/auth/v1/auth.proto, Docker: /app/proto/auth/v1/auth.proto
- ✅ Global exception filter (filters/grpc-exception.execption.filter.ts) — maps gRPC errors → HTTP
- ✅ Dockerfile (multi-stage)
- Port: 3001 (configurable via PORT)
Proto (proto/)
- ✅ proto/auth/v1/auth.proto — shared by Go auth + NestJS gRPC client
Database
- ✅ Migrations: services/migrations/000001_create_users_table.up.sql + .down.sql
- ✅ Managed by golang-migrate/migrate container in compose
Docker Compose (docker-compose.yml)
- postgres — PostgreSQL 17, healthcheck, named volume moon-panel-data
- auth — Go auth service, depends on postgres healthy
- bff — NestJS, depends on auth, port 3001
- migrate — runs migrations on setup (profile: setup)
Architecture (Actual)
Moon Panel is a Laravel hosting control panel (like CloudPanel). React SPA → nginx → NestJS BFF → Go microservices (auth, config, deploy) + PostgreSQL + MySQL.
Started But Not Wired
- BFF -> auth connection needs testing with Docker compose (port mismatch issue: curl to 3001, not 3000)
- Global exception filter doesn't log errors — needs console.error added
Suggested Next Steps
1. Fix port: curl localhost:3001 or change compose to port 3000
2. Add console.error(exception) in the exception filter for debugging
3. Test auth flow end-to-end with Docker compose
4. Build Go Config Service — generates nginx vhost configs
5. Build NestJS Sites module — CRUD for Laravel sites
6. Build React frontend — login + dashboard
7. Eventually: deploy service, event scripts, webhook system
